// HeroHome.test.js

import React from 'react';
import { HeroHome } from './HeroHome';  // Asegúrate de importar correctamente tu componente
import { render } from '@testing-library/react';

describe('HeroHome Component', () => {
    it('renders correctly', () => {
        render(<HeroHome />);
    
  });

 

});
